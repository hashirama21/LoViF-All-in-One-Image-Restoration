torch>=2.3.0
torchvision>=0.18.0
torchmetrics>=0.11.0
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.29.0
peft>=0.10.0
omegaconf>=2.3.0
hydra-core>=1.3.2
lpips>=0.1.4
Pillow>=10.0.0
tqdm>=4.66.0
wandb>=0.16.0
pytest>=8.0.0

# Optional — memory-efficient attention on NVIDIA GPUs only:
# xformers>=0.0.25

# Optional — MUSIQ + CLIP-IQA reward metrics for DPO pair generation:
# pyiqa>=0.1.10
